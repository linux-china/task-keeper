initscript {
    repositories {
        gradlePluginPortal()
    }
    dependencies {
        classpath 'com.github.ben-manes:gradle-versions-plugin:0.61.0'
    }
}

allprojects {
    apply plugin: com.github.benmanes.gradle.versions.VersionsPlugin
}
